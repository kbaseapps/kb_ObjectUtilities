#
# define display information
#
name: Bulk Hide Data Objects - v1.2.0
tooltip: |
    Allows user to hide data objects by type

screenshots: []

icon: kb-orange.png

#
# define a set of similar methods that might be useful to the user
#
suggestions:
    apps:
        related:
            []
        next:
            []
    methods:
        related:
            []
        next:
            []

#
# Configure the display and description of parameters
#
parameters :
    object_types:
        ui-name : |
            Object Type(s)
        short-hint : |
            The object types to hide (e.g. Genome, Assembly)
    verbose:
        ui-name : |
            Verbose
        short-hint : |
            List objects as they are hidden
    hide_all:
        ui-name : |
            Hide All
        short-hint : |
            Hide all objects of known types (Note: USE WITH CAUTION!!!)

description : |
    <p>This method allows the user to hide objects of a certain type or types in the workspace.</p>
